// Placeholder landing page for the initial scaffold — replaced by the real
// Home screen (search, category shortcuts, map entry point) in the next
// build step (Tech Spec §4.1).
export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-brand-900 px-6 text-center text-white">
      <h1 className="text-3xl font-bold">LIBERIA360</h1>
      <p className="text-brand-100">Everything Liberia. One Place.</p>
      <p className="max-w-sm text-sm text-brand-200">Frontend scaffold is wired up — Home screen build is next.</p>
    </main>
  );
}
